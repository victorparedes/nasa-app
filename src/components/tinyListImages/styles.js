import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        marginVertical: 8,
    },
    titleContainer: {
        marginBottom: 10,
    },
    content: {
        paddingHorizontal: 15,
    },
});
