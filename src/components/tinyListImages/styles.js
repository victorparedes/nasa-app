import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        marginVertical: 8,
    },
    title: {
        color: '#fff',
        fontSize: 20,
        marginBottom: 10,
    },
    content: {
        paddingHorizontal: 15,
    },
});
